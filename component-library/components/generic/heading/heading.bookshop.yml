# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: "Heading"
  description:
  icon:
  tags: []

# Defines the structure of this component, as well as the default values
blueprint:
  title: 
  subtitle: 
  title_color: 
  subtitle_color: 
  line_color: 

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:
  title: "Title"
  subtitle: "Dantes remained confused and silent by this explanation of the thoughts which had unconsciously"
  title_color: "primary-1"
  subtitle_color: "primary-1"
  line_color: "primary-1"

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
  title_color:
    type: select~
    options:
      values:
        - "primary-1"
        - "primary-2"
        - "primary-3"
        - "white"
        - "black"
  subtitle_color:
    type: select
    options:
      values:
        - "primary-1"
        - "primary-2"
        - "primary-3"
        - "white"
        - "black"
  line_color:
    type: select
    options:
      values:
        - "primary-1"
        - "primary-2"
        - "primary-3"
        - "white"
        - "black"
     
